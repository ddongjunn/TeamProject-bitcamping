<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
    <mapper namespace="com.camping.bit.dao.AdminDao">

    <select id="allMember" resultType="MemberDto">
        SELECT ID, USERNAME, PASSWORD, NICKNAME, PHONE, EMAIL, CREATE_DATE, SNS_TYPE FROM MEMBER WHERE AUTH = 0
    </select>

    <select id="memberCount" resultType="int">
        SELECT COUNT(*) FROM MEMBER WHERE AUTH = 0
    </select>

  </mapper>